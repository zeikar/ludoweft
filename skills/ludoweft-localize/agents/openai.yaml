interface:
  display_name: "Ludoweft Localize"
  short_description: "Localize moddable games with Ludoweft agents"
  default_prompt: "Use $ludoweft-localize to inspect this authorized game localization project and continue the next safe workflow stage."
policy:
  allow_implicit_invocation: true
