{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ludoweft.dev/schemas/segment.schema.json",
  "title": "Ludoweft translation segment",
  "type": "object",
  "required": ["id", "source", "target", "sourceHash"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "source": { "type": "string" },
    "reference": { "type": "string" },
    "target": { "type": "string" },
    "previousSource": { "type": "string" },
    "previousStatus": {
      "enum": ["untranslated", "draft", "translated", "reviewed", "blocked", "stale"]
    },
    "sourceHash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "protectedTokens": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "protectedTokenSource": { "enum": ["source", "reference"] },
    "protectedTokenProfile": { "enum": ["default", "mages"] },
    "context": { "type": "object" },
    "status": {
      "enum": ["untranslated", "draft", "translated", "reviewed", "blocked", "stale", "orphaned"]
    },
    "translatedBy": { "type": "string" },
    "reviewedBy": { "type": "string" }
  },
  "additionalProperties": true
}
